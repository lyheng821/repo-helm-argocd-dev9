apiVersion: v1
kind: Service
metadata:
  name: {{ include "reactjs-plants-charts.fullname" . }}
  labels:
    {{- include "reactjs-plants-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reactjs-plants-charts.selectorLabels" . | nindent 4 }}
